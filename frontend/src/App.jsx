import{useState}from'react';import Dashboard from'./pages/Dashboard';import Reorder from'./pages/Reorder';import Excess from'./pages/Excess';import Expiry from'./pages/Expiry';import Inventory from'./pages/Inventory';
export default function App(){const[p,setP]=useState('dashboard');const pages={dashboard:<Dashboard/>,reorder:<Reorder/>,excess:<Excess/>,expiry:<Expiry/>,inventory:<Inventory/>};return <div className="app"><aside><h1>PharmaOpt</h1><div className="sideSub">Inventory Optimization</div>{[['dashboard','Dashboard'],['reorder','Reorder'],['excess','Excess Stock'],['expiry','Near Expiry'],['inventory','Inventory']].map(([k,l])=><button className={p===k?'active':''} onClick={()=>setP(k)} key={k}>{l}</button>)}</aside><main><header><div><h2>Pharmacy Inventory Optimization</h2><p>Phase 1 · Rule-based decision support</p></div></header><section>{pages[p]}</section></main></div>}
